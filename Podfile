# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestClient' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for TestClient
  pod 'iStream', '~> 0.2.0'
  
  target 'TestClientTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'TestClientUITests' do
    # Pods for testing
  end
  
end
